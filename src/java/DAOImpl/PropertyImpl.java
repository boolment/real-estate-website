package DAOImpl;

import DAOInterface.PropertyInterface;
import org.apache.log4j.Logger;

public class PropertyImpl implements PropertyInterface {

    private static final Logger LOG = Logger.getLogger(PropertyImpl.class);
}
