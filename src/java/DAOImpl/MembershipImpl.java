package DAOImpl;

import DAOInterface.MembershipInterface;
import org.apache.log4j.Logger;

public class MembershipImpl implements MembershipInterface {

    private static final Logger LOG = Logger.getLogger(PgImpl.class);
}
